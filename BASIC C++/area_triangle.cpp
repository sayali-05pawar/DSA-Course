#include <iostream>
using namespace std;

int main()
{
    int len,height;
    cout<<"Enter the length of triangle: ";
    cin>>len;
    cout<<"Enter the height of triangle: ";
    cin>>height;
    cout<<"Area of triangle is "<<0.5*len*height;
    return 0;
}
